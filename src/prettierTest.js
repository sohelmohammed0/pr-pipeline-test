export const add = (a, b) => {
  return a + b;
};

export const multiply = (a, b) => {
  return a * b;
};

export const user = { name: 'John', age: 25, city: 'NY' };

export function greet(name) {
  console.log('Hello ' + name);
}
